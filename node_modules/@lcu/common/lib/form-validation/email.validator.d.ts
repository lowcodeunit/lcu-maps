/**
 * @dynamic need this because there are static memebers
 */
export declare class EmailValidator {
    /**
     * Email regular expression pattern, requires domain
     */
    static readonly EmailPatternDomain: string;
}
