import { OnInit, Injector } from '@angular/core';
export declare abstract class LcuElementComponent<T> implements OnInit {
    protected injector: Injector;
    context: T;
    constructor(injector: Injector);
    ngOnInit(): void;
    SetContext(ctxt: T): void;
}
