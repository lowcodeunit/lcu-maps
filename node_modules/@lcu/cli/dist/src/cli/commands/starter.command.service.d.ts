import { BaseCommandService } from "./BaseCommandService";
import { Command } from "commander";
export declare class StarterCommandService extends BaseCommandService {
    protected slnName: string;
    constructor();
    Setup(program: Command): Promise<Command>;
    protected initialize(context: any): Promise<void>;
    protected setupProject(context: any): Promise<void>;
    protected setupSolution(context: any): Promise<void>;
    protected ensureName(name: string): Promise<string>;
    protected establishTemplatesRepo(repoTempPath: string, repo: string): Promise<{}>;
    protected processTemplates(context: any, subPath: string): Promise<void>;
}
