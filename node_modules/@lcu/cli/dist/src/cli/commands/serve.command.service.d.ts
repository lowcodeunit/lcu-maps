import { BaseCommandService } from './BaseCommandService';
import { Command } from 'commander';
export declare class ServeCommandService extends BaseCommandService {
    constructor();
    Setup(program: Command): Promise<Command>;
    protected processFileChange(filePath: string, host: string, app: string, outputPath: string, shouldDelete?: boolean): void;
}
